<Window x:Class="CyberChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Chatbot"
        Height="650" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#16213e" Padding="20">
            <StackPanel>
                <TextBlock Text="Cybersecurity Awareness Chatbot" 
                           FontSize="22" FontWeight="Bold" 
                           Foreground="#00ff96"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Cybersecurity Awareness Assistant" 
                           FontSize="13" 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
                <TextBlock Text="Your Personal Security Guide" 
                           FontSize="11" 
                           Foreground="#888888"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Button Bar -->
        <Border Grid.Row="1" Background="#16213e" Padding="15" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnHello" Content="Hello" Width="80" Margin="5" Click="BtnHello_Click"/>
                <Button x:Name="BtnClearChat" Content="Clear" Width="80" Margin="5" Click="BtnClearChat_Click"/>
                <Button x:Name="BtnShowImages" Content="Show Images" Width="100" Margin="5" Click="BtnShowImages_Click"/>
            </StackPanel>
        </Border>

        <!-- Chat Area -->
        <Border Grid.Row="2" Background="#1a1a2e" Margin="10">
            <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ChatPanel" Margin="10"/>
            </ScrollViewer>
        </Border>

        <!-- Input Area -->
        <Border Grid.Row="3" Background="#0f3460" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TxtUserInput" 
                         Grid.Column="0"
                         Background="White"
                         Foreground="#333"
                         BorderBrush="#ddd"
                         BorderThickness="1"
                         Padding="15"
                         FontSize="14"
                         KeyDown="TxtUserInput_KeyDown"/>

                <Button x:Name="BtnSend" 
                        Grid.Column="1"
                        Content="SEND"
                        Background="#00ff96"
                        Foreground="#1a1a2e"
                        FontWeight="Bold"
                        Padding="25,12"
                        Margin="10,0,0,0"
                        Click="BtnSend_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>